apiVersion: v1
kind: Service
metadata:
  name: llm-proxy
  namespace: etymograph
  labels:
    app: llm-proxy
spec:
  selector:
    app: llm-proxy
  ports:
    - name: http
      port: 8081
      targetPort: 8081
  type: ClusterIP

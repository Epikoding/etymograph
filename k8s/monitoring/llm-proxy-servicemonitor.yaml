# ServiceMonitor for EtymoGraph LLM Proxy
# Prometheus Operator가 이 리소스를 감지하여 자동으로 메트릭을 수집합니다.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: etymograph-llm-proxy
  namespace: monitoring
  labels:
    app: llm-proxy
    release: prometheus
spec:
  selector:
    matchLabels:
      app: llm-proxy
  namespaceSelector:
    matchNames:
      - etymograph
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
